# Food Chain Sphere

3D spherical ecosystem simulation with plants, herbivores, carnivores, population graphs, and inherited speed traits.

## Play

Open `index.html` directly, or publish this folder with GitHub Pages.

## GitHub Pages

Use `Settings` -> `Pages` -> `Deploy from a branch`, then select `main` and `/ (root)`.
